"""Vendor adapters (plug-ins). Each module self-registers via @register.

Adding a vendor = add ONE file here. Nothing else in the framework or the
Troubleshooting Engine changes. These reference adapters are ILLUSTRATIVE and
intentionally minimal ("including but not limited to").
"""
